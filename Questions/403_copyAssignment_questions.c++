/**
 * 1. What does this line do in my_vector?
 *
 * 		std::fill(begin(), end(), v);
 * 
 * 2. What does the "explicit" keyword do?
 *
 * 3. What does this line do in my_vector?
 *
 * 		std::copy(that.begin(), that.end(), begin());
 *
 * 4. What does this line do in my_vector?
 *
 * 		std::swap(_s, that._s);
 *
 * 5. Does operator= in my_vector take in a my_vector by reference or not? Why?
 * 		Does it return by reference? Why?
 *
 * 
 */