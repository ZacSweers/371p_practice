1. forces compiler to check whether you've actually overridden the method or not

2. C++
	"using <className>::<methodName>;"

3. Uses B's (because it doesn't specify to use A's)

4. Uses A's (because it specifies to use A's)

5. Uses's A's (because it explicitly calls A's)

