/**
 * DyanmicBinding.java Questions - Solutions
 * =========================================
 * 1. In Java, is dynamic binding enabled or disabled by default?
 * A: Enabled.
 *
 * 2. In what 4 cases is dynamic binding turned off in Java?
 * A: 1) The method is marked static.
 *    2) The method is marked private. 
 *    3) The method is marked final.
 *    4) The class is marked final. 
 *
 * 3. What advantage is there to dynamic binding being turned off? What can the compiler then do?
 * A: If static binding is used, the compiler can inline the method, which is a performance gain. 
 * /
